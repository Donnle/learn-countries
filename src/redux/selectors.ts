export const userIdSelector = ({user}: any) => user.userId

export const userDataSelector = ({user}: any) => user.userData
export const loadingSelector = ({user}: any) => user.loading
export const loadedSelector = ({user}: any) => user.loaded
